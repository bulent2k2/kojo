diff --git a/scalariform/src/main/scala/scalariform/lexer/Keywords.scala b/scalariform/src/main/scala/scalariform/lexer/Keywords.scala
index eada2b2..443dc84 100644
--- a/scalariform/src/main/scala/scalariform/lexer/Keywords.scala
+++ b/scalariform/src/main/scala/scalariform/lexer/Keywords.scala
@@ -46,45 +46,44 @@ object Keywords {
     "while" -> WHILE,
     "with" -> WITH,
     "yield" -> YIELD,
-    "soyut" -> ABSTRACT,
-    "durum" -> CASE,
-    "yakala" -> CATCH,
-    "sınıf" -> CLASS,
-    "tanım" -> DEF,
-    "yap" -> DO,
-    "yoksa" -> ELSE,
-    "yayar" -> EXTENDS,
-    "yanlış" -> FALSE,
-    "son" -> FINAL,
-    "sonunda" -> FINALLY,
-    "için" -> FOR,
-    "bazı" -> FORSOME,
-    "eğer" -> IF,
-    "örtük" -> IMPLICIT,
-    "getir" -> IMPORT,
-    "miskin" -> LAZY,
-    "eşle" -> MATCH,
-    "yeni" -> NEW,
-    "yok" -> NULL,
-    "nesne" -> OBJECT,
-    "baskın" -> OVERRIDE,
-    "deste" -> PACKAGE,
-    "gizli" -> PRIVATE,
-    "koru" -> PROTECTED,
-    "geriDön" -> RETURN,
-    "damgalı" -> SEALED,
-    "üst" -> SUPER,
-    "bu" -> THIS,
-    "bildir" -> THROW,
-    "özellik" -> TRAIT,
-    "dene" -> TRY,
-    "doğru" -> TRUE,
-    "tür" -> TYPE,
-    "dez" -> VAL,
-    "den" -> VAR,
-    "yineleDoğruKaldıkça" -> WHILE,
-    "birlikte" -> WITH,
-    "ver" -> YIELD,
+    // Swedish keywords (added for Kojo's Swedish edition; English still works)
+    "abstrakt"  -> ABSTRACT,
+    "fall"      -> CASE,
+    "fånga"     -> CATCH,
+    "klass"     -> CLASS,
+    "definiera" -> DEF,
+    "utför"     -> DO,
+    "annars"    -> ELSE,
+    "ärver"     -> EXTENDS,
+    "falskt"    -> FALSE,
+    "slutlig"   -> FINAL,
+    "slutligen" -> FINALLY,
+    "för"       -> FOR,
+    "förNågra"  -> FORSOME,
+    "om"        -> IF,
+    "importera" -> IMPORT,
+    "lat"       -> LAZY,
+    "matcha"    -> MATCH,
+    "ny"        -> NEW,
+    "ingen"     -> NULL,
+    "objekt"    -> OBJECT,
+    "åsidosätt" -> OVERRIDE,
+    "paket"     -> PACKAGE,
+    "privat"    -> PRIVATE,
+    "skyddad"   -> PROTECTED,
+    "returnera" -> RETURN,
+    "förseglad" -> SEALED,
+    "denna"     -> THIS,
+    "kasta"     -> THROW,
+    "egenskap"  -> TRAIT,
+    "sant"      -> TRUE,
+    "försök"    -> TRY,
+    "typ"       -> TYPE,
+    "värde"     -> VAL,
+    "variabel"  -> VAR,
+    "medan"     -> WHILE,
+    "med"       -> WITH,
+    "ge"        -> YIELD,
     "_" -> USCORE,
     ":" -> COLON,
     "=" -> EQUALS,
