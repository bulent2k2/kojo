diff --git a/scalariform/src/main/scala/scalariform/lexer/Keywords.scala b/scalariform/src/main/scala/scalariform/lexer/Keywords.scala
index eada2b2..c1602ee 100644
--- a/scalariform/src/main/scala/scalariform/lexer/Keywords.scala
+++ b/scalariform/src/main/scala/scalariform/lexer/Keywords.scala
@@ -85,6 +85,44 @@ object Keywords {
     "yineleDoğruKaldıkça" -> WHILE,
     "birlikte" -> WITH,
     "ver" -> YIELD,
+    // Swedish keywords (added for Kojo's Swedish edition; English still works)
+    "abstrakt"  -> ABSTRACT,
+    "fall"      -> CASE,
+    "fånga"     -> CATCH,
+    "klass"     -> CLASS,
+    "definiera" -> DEF,
+    "utför"     -> DO,
+    "annars"    -> ELSE,
+    "ärver"     -> EXTENDS,
+    "falskt"    -> FALSE,
+    "slutlig"   -> FINAL,
+    "slutligen" -> FINALLY,
+    "för"       -> FOR,
+    "förNågra"  -> FORSOME,
+    "om"        -> IF,
+    "importera" -> IMPORT,
+    "lat"       -> LAZY,
+    "matcha"    -> MATCH,
+    "ny"        -> NEW,
+    "ingen"     -> NULL,
+    "objekt"    -> OBJECT,
+    "åsidosätt" -> OVERRIDE,
+    "paket"     -> PACKAGE,
+    "privat"    -> PRIVATE,
+    "skyddad"   -> PROTECTED,
+    "returnera" -> RETURN,
+    "förseglad" -> SEALED,
+    "denna"     -> THIS,
+    "kasta"     -> THROW,
+    "egenskap"  -> TRAIT,
+    "sant"      -> TRUE,
+    "försök"    -> TRY,
+    "typ"       -> TYPE,
+    "värde"     -> VAL,
+    "variabel"  -> VAR,
+    "medan"     -> WHILE,
+    "med"       -> WITH,
+    "ge"        -> YIELD,
     "_" -> USCORE,
     ":" -> COLON,
     "=" -> EQUALS,
